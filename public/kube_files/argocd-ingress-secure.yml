apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-ingress
  namespace: argocd
  annotations:
    # 1. Get a proper Let's Encrypt certificate
    cert-manager.io/cluster-issuer: "letsencrypt-cloudflare"

    # 2. Tell Cloudflare to create the DNS record
    external-dns.alpha.kubernetes.io/hostname: "your-argocd-domain.com"

    # 3. Point the DNS record to your private Tailscale IP
    external-dns.alpha.kubernetes.io/target: "YOUR_TAILSCALE_IP"

    # 4. CRITICAL: Turn OFF the Cloudflare Proxy (Grey Cloud)
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
spec:
  tls:
    - hosts:
        - your-argocd-domain.com
      secretName: argocd-tls-secret # Cert-Manager creates this
  rules:
    - host: your-argocd-domain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
